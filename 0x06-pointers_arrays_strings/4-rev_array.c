#include "main.h"
/**
 * reverse_array - compares two strings
 * @a: array
 * @n: number of elements
 * Return: the result
 */
void reverse_array(int *a, int n)
{
int i;

for (i = a[n - 1]; i >= a[0]; i--)
return (a);
}

