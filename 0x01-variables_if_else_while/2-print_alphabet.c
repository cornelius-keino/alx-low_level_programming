#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'print characters'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int ch;

for (ch = 'a'; ch <= 'z'; ch++)
{
putchar (ch);
}
putchar ("\n")
return (0);
}
