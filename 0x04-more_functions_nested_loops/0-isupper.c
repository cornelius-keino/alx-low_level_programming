#include "main.h"
/**
 * _isupper - check if character is upper case
 * @c: variable to cheked if upper
 *
 * Return: 1 or 0 depending with statement
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}

