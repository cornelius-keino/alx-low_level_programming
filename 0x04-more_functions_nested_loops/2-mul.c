#include "main.h"
/**
 * mul - multiply two numbers
 * @a: first number
 * @b: second number
 * Return: 1 or 0 depending with statement
 */
int mul(int a, int b)
{
int result;

result = a * b;
return (result);
}
